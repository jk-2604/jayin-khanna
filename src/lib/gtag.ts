// src/lib/gtag.ts
export const GA_TRACKING_ID = "G-WS60KHCCGX";

// You can add other helper functions here if needed for custom events
// export const event = ({ action, category, label, value }) => {
//   if (typeof window.gtag === 'function') {
//     window.gtag('event', action, {
//       event_category: category,
//       event_label: label,
//       value: value,
//     });
//   }
// };
